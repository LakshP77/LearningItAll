/*

public class stringMethods {
    public static void main(String[] args) {

        // String methods
        
        String name = "Laksh Patel";

        // int length = name.length();
        // char letter = name.charAt(3);
        // int index = name.indexOf("a");
        // int lastIndex = name.lastIndexOf("a");

        // name = name.toUpperCase();
        // name = name.toLowerCase();
        // name = name.trim();
        // name = name.replace("a", "o");
        
       
        if(name.isEmpty()) {
            System.out.println("You name is empty.");
        } else {
            System.out.println("Hello " + name);
        }

        // System.out.println(length);
        // System.out.println(letter);
        // System.out.println(index);
        // System.out.println(name);
        

        
        if(name.contains(" ")) {
            System.out.println("Your name has a space");
        } else {
            System.out.println("You name has no spaces");
        }

        

        if(name.equals("password")) {
            System.out.println("You are a password ig");

        } else {
            System.out.println("You are not a password");
        }
    
    }
}

*/
